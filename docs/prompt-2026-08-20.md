# Промпт новой сессии · 2026-08-20

Скопировать в новый чат Cursor целиком.

---

Craft · Kraftum Migration Engine. Репозиторий `/var/www/www-root/data/www/craft.nordic-builder.ru`. Прод https://craft.nordic-builder.ru. Не путать с конструктором nordic-builder.ru и с leads.konversus.ru.

Сначала прочитай: `AGENTS.md`, `docs/plan-2026-08-20.md`, `docs/live-editor.md`, `docs/architecture.md`, `docs/decisions.md` (ADR-0029…0032), `docs/devlog.md` (закрытие 19.08).

Правила: Puck не рендерит сайт — Legacy HTML + Patch Model. Playwright только для импорта. Не коммитить `.env`, `storage/`, `www.zip`. После правок portable копировать в `/var/www/www-root/data/www/demo.nordic-builder.ru/` и `pm2 restart craft-demo-polygon`. Полигон: https://demo.nordic-builder.ru/admin. practic-hub.ru не перезаписывать.

GitHub уже настроен: deploy key `craft-vps`, `origin` = `git@github.com-craft:ztim2008/craft.git`. Push с VPS работает. Не делать новый ключ и не менять `git config user.*`. Не `--force`.

Сегодня по плану:

1. Приёмка полигона глазами клиента, чинить только блокеры.
2. Слой 3 ADR-0030: добавить страницу — клон HTML-каркаса + пункт меню на новый путь, публикация в public/, проверка на полигоне.

Не делать: ЮKassa, AI, Agent API, Telegram/Sheets, импорт sx7238, `#craft-dbg`.

В конце дня: devlog, коммит, `git push origin main`.

Начни с приёмки полигона (пункт 1 плана). Не начинай с GitHub.
