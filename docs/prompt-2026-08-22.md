# Промпт новой сессии · после 21.08

Скопировать в новый чат Cursor целиком.

---

Craft · Kraftum Migration Engine. Репозиторий `/var/www/www-root/data/www/craft.nordic-builder.ru`. Прод https://craft.nordic-builder.ru. Не путать с конструктором nordic-builder.ru и с leads.konversus.ru.

Сначала прочитай: `AGENTS.md`, `docs/plan-2026-08-22.md`, `docs/live-editor.md`, `docs/architecture.md`, `docs/decisions.md` (ADR-0029…0034), `docs/devlog.md` (закрытие 21.08).

Правила: Puck не рендерит сайт — Legacy HTML + Patch Model. Playwright только для импорта. Не коммитить `.env`, `storage/`, `www.zip`, `.originkit/`. После правок portable копировать в `/var/www/www-root/data/www/demo.nordic-builder.ru/` и `pm2 restart craft-demo-polygon`. Полигон: https://demo.nordic-builder.ru/admin. practic-hub.ru не перезаписывать.

GitHub: deploy key `craft-vps`, `origin` = `git@github.com-craft:ztim2008/craft.git`. Не новый ключ, не `git config user.*`, не `--force`.

Сейчас этап **набора миграций**: чинить только дыры с живых сайтов. Статьи, дизайн-блок + агент — обсуждение, не старт фичи без запроса. Конструктор Craftum не клонировать. ЮKassa / AI / Agent API — не этот этап.

Коммит и push — только если попросят.
