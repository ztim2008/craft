# План на 2026-08-20 · утверждён 19.08 вечером

Цель дня: не расползаться. Live-editor на полигоне уже умеет страницу, шапку, меню, подвал, HTML-код и виджет `.pic`. Завтра даём **новую страницу** и принимаем полигон глазами клиента.

Полигон: https://demo.nordic-builder.ru/admin · шпаргалка: [live-editor.md](./live-editor.md)

## Делать

1. **Приёмка полигона как клиент** (коротко, только блокеры)
   - Правка текста на странице → сохранить → опубликовать → видно на сайте
   - Пункт меню (клон) → десктоп и мобилка
   - Шапка / подвал / HTML-код / виджеты — канвас только этот chrome
   - Заявка с формы → инбокс `/admin`
   - Баги чинить сразу; косметику откладывать

2. **Слой 3 (ADR-0030): добавить страницу**
   - Клон HTML-каркаса (шапка+подвал+пустой/минимальный контент), не React-шаблон
   - Новый путь в `page-model`
   - Пункт меню на этот путь (уже умеем клонировать `menu-item`)
   - Публикация собирает файл в `public/`
   - Проверка на полигоне: новая страница открывается, в меню есть ссылка, внутренние URL относительные

3. **Документация в конце дня** — devlog + live-editor, если API страницы изменится

## Не делать 20.08

- ЮKassa, AI-секции, Agent API
- Telegram / Google Sheets
- Второй импорт sx7238 (формы) — после того как «добавить страницу» поедет
- Puck как renderer, Playwright «потыкать админку»
- Включать `#craft-dbg`

## Если не успеем всё

Минимум сдачи: приёмка без блокеров + черновик «добавить страницу» (модель + патч, даже без полировки UI). sx7238 — следующий рабочий день после страницы.
