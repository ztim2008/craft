# План на 2026-08-20 · утверждён 19.08 вечером

Цель дня: приёмка полигона глазами клиента, затем **добавить страницу** (ADR-0030 слой 3). Live-editor уже умеет страницу, шапку, меню, подвал, HTML-код и виджет `.pic`.

GitHub с VPS **уже работает** (19.08 ночью): deploy key `craft-vps`, remote `git@github.com-craft:ztim2008/craft.git`. Не генерировать второй ключ.

Полигон: https://demo.nordic-builder.ru/admin · шпаргалка: [live-editor.md](./live-editor.md)  
Промпт сессии: [prompt-2026-08-20.md](./prompt-2026-08-20.md)

## Делать

1. **Приёмка полигона как клиент** (только блокеры)
   - Правка текста → сохранить → опубликовать → видно на сайте
   - Пункт меню (клон) → десктоп и мобилка
   - Шапка / подвал / HTML-код / виджеты — на канвасе только этот chrome
   - Заявка с формы → инбокс `/admin`
   - После portable: копия на demo + `pm2 restart craft-demo-polygon`

2. **Слой 3 (ADR-0030): добавить страницу**
   - Клон HTML-каркаса (шапка+подвал+минимальный контент), не React
   - Новый путь в `page-model`
   - Пункт меню на этот путь
   - Публикация пишет файл в `public/`
   - На полигоне: страница открывается, в меню ссылка, URL относительные

3. **Конец дня** — devlog, коммит, `git push origin main` (без force)

## Не делать 20.08

- ЮKassa, AI, Agent API, Telegram / Sheets
- Импорт sx7238 — после «добавить страницу»
- Puck как renderer, Playwright по редактору, `#craft-dbg`
- Новый GitHub remote / второй deploy key
- Не трогать `git config user.*`

## Минимум сдачи

Приёмка без блокеров. Черновик «добавить страницу» — если останется время.
